<%u = Potin.find("#{@id}")%>
<% i = u.Title%>
<% j = u.Content%>

<div class="card">
 <div class="card-header">
   <h4><em><b>Voici le potin numéro <%= "#{@id}"%></b></em></h4>
 </div>
 <div class="card-body">
   <blockquote class="blockquote mb-0">
     <p><%= "#{j}"%></p>
     <footer class="blockquote-footer"><%="#{i}"%></footer>
   </blockquote>
 </div>
</div>